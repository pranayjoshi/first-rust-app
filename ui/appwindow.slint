import { Button, VerticalBox, LineEdit, HorizontalBox} from "std-widgets.slint";

component CalculateButton inherits Button {
    text: "Calculate";
    // on-clicked: request-increase-value();
    primary: true;
    
}

export component AppWindow inherits Window {
    property <string> name;
    callback divide-income(int, inc);
    background: white;
    title: "Income Tax Calculator";
    VerticalBox {
        padding: 50px;
        spacing: 25px;
        HorizontalLayout {
            Text {
                text: "Enter Total Income";
                horizontal-alignment: center;
                font-size: 24px;
                font-weight: 900;
            }
        }

        HorizontalLayout {
            income := LineEdit {
                horizontal-alignment: center;
                font-size: 16px;
                placeholder-text: "Enter your number";
            }
        }
        HorizontalLayout{
            CalculateButton {
                on-clicked: divide-income(income.text);
            }
        }
        VerticalBox {}
    }
}
